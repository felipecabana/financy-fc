type User {
  id: String!
  name: String!
  email: String!
  createdAt: String!
  updatedAt: String!
}

input UpdateUserInput {
  name: String!
}

extend type Query {
  me: User!
}

extend type Mutation {
  updateUser(data: UpdateUserInput!): User!
}
